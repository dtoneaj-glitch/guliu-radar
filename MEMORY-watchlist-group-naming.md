---
name: watchlist-group-naming
description: guliu-radar 自選股預設分類更名（觀察股/水池），及延後處理的庫存股額外欄位構想
metadata:
  type: project
---

## 變更內容（2026-09-10）

使用者釐清「觀察股」跟「庫存股」的概念，發現原本 4 個預設分類（主流/蓄勢/乘流/靜流）裡，「乘流」（已進場持有中）沒有直接表達出「庫存股」的意思，跟產品整體「大白話」原則不一致。

`client/src/lib/watchlist.ts` 的 `PRESET_GROUPS` 只改 `name`（顯示名稱），`id` 不變，舊資料不需要遷移：

| id | 舊 name | 新 name | 意思 |
|---|---|---|---|
| main | 主流 | **觀察股** | 核心關注，每天都要看 |
| charge | 蓄勢 | 蓄勢（不變） | 準備進場，等條件成熟 |
| ride | 乘流 | **水池** | 已進場持有中（庫存股）——選「水池」（裝著、不流動）比「乘流」（正在衝浪的動感）更貼近「已經持有、放著」的語意，同時維持水流意象跟其他分類一致 |
| still | 靜流 | 靜流（不變） | 暫不關注 |

確認過遷移邏輯（v1 純代號清單自動轉入）跟其他程式碼都用 `id: "main"` 比對，沒有用顯示名稱字串比對，改名不影響既有邏輯。

## 延後處理：庫存股額外欄位

使用者提出「水池」（庫存股）分類可能需要額外資料才能真正發揮用途：
- 買入成本價（用來算損益）
- 持有股數
- 買入日期

目前 `WatchItem { symbol, groups }` 只有代號跟分類標籤，沒有這些欄位。使用者說法是「這建議不錯，先保留」——明確表示先不做，記錄下來供之後評估。這是一個真正的功能擴充（甚至涉及要不要做損益試算），不是單純的命名調整。

## 驗證

- `tsc --noEmit` 0 錯誤、`vitest run` 41/41 全過（不受影響）
- 黑盒驗證 `PRESET_GROUPS` 實際輸出：`[{id:'main',name:'觀察股'}, {id:'charge',name:'蓄勢'}, {id:'ride',name:'水池'}, {id:'still',name:'靜流'}]`
