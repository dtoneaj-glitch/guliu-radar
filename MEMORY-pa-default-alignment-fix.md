---
name: pa-default-alignment-fix
description: guliu-radar pa-default.ts 六段研判引擎兩處邏輯修正——多時框對齊分數改為真正決定觸發、支撐緩衝區收窄
metadata:
  type: project
---

## 背景（2026-09-12）

使用者要求「作多為主」（不做空），檢視 `shared/pa-default.ts`（PA 六段研判引擎）觸發邏輯時發現兩個問題。

## 修正 1：多時框對齊分數（`bundle.alignment.score`）算了、顯示了，但沒有真的拿來決定要不要觸發

第一段「判斷市場結構」會顯示 -100~+100 的加權對齊分數（週線佔比最重 40%），但原本 `triggered` 的判斷條件完全沒有用到這個分數——只看日線趨勢、是否靠近支撐、多頭形態、15分資料是否齊備。等於「顯示給使用者看的資訊」跟「引擎自己做決策的邏輯」是脫節的。

**修法**：新增 `alignmentSupportsLong = bundle.alignment.score != null && bundle.alignment.score > 0`，作為 `triggered` 的額外必要條件。資料不足（score 為 null）時維持保守不觸發，跟其餘欄位「不足就不給結論」的原則一致。

**探測時發現的意外行為**：`computeAlignmentScore()` 在只有單一時框（例如只有日線）ready 時，**score 不會是 null**，會直接反映那個時框自己的方向（例如純上升結構會給 100 分）。null 只有在完全沒有任何時框 ready 時才會出現。這代表在「只有日線資料」這種很常見的呼叫情境下，這個新加的門檻不會額外限制什麼（score 就等於日線方向本身），要有第二個以上時框資料、且方向跟日線衝突時，這個門檻才會真的發揮作用擋下不一致的訊號。

## 修正 2：「靠近支撐」的判斷區間上下不對稱，偏寬鬆

```
close >= support.low - (high-low)*0.5    ← 下緣，往下多抓 50%（允許插針跌破後反彈）
close <= support.high + (high-low)*0.75  ← 上緣，往上多抓 75%（改動前）
```

上緣比下緣多抓快 1.5 倍，代表股價已經從支撐反彈、往上走了快一個支撐區高度的 75%，還會被判定成「剛好在支撐附近」——這樣「靠近支撐」的判斷會太寬鬆，容易誤把已經反彈一段的價位算進去。

**修法**：上緣從 0.75 收窄到 0.35，下緣維持 0.5 不變（允許插針但不放寬反彈後的判斷範圍）。

## 補的測試

`pa-default.ts` 之前完全沒有針對 `triggered` 邏輯的專屬測試（只有 1 個測試檢查六段結構完整性）。這次補了 2 個：
- 只有日線資料時，score 不是 null（反映日線自身方向），不會因為「分數不足」被誤判
- 週線/60分/15分為真正下降結構（LH/LL 擺盪，不是單調遞減）、日線上升時，score 應為負，此時不應該觸發

寫測試過程中也抓到自己第一版寫錯：單調遞減的收盤價不會被 `buildMarketFacts` 判定成「下降」結構（會落到「震盪」），要用跟既有 `uptrend()` 一樣的錨點擺盪寫法（高低點交替）才會真的產生 LH/LL 的下降結構——已建立對稱的 `downtrend()` 測試 helper。

## 驗證

- `tsc --noEmit` 0 錯誤
- `vitest run` 47/47 全過（原 45 + 新增 2）
- 「做空邏輯」（發現 3）這次刻意不處理——使用者確認產品策略是「作多為主」，不需要對稱的空頭觸發條件
