---
name: strategy-stoploss-gaps
description: guliu-radar 9個戰法的停損邏輯盤點與補齊（順流/潮間帶/破浪/潮流），含移動停利待辦
metadata:
  type: project
---

## 背景（2026-09-12）

使用者發現：研判引擎跟戰法主要在講「怎麼進場」，沒講「怎麼出場／移動停利」。查證後盤點 9 個戰法：

| 戰法 | 原本狀態 |
|---|---|
| 聲納／回湧／破堤／鯨躍／洋流 | ✅ 已有明確停損（聲納委託六段框架的失效位，其餘4個用「失效：xxx」文字明講） |
| **順流、潮間帶** | ❌ 完全沒有 |
| **破浪、潮流** | 邏輯本身已用失效位/HL把關（`f.invalidation`／`lastLow`），但觸發時的提示文字沒把具體數字講出來給使用者看 |

第一次盤點時關鍵字搜尋漏掉「失效」這個詞（只搜了「止損/停損/出場/止盈/停利」），誤判成 4 個戰法都沒有——這是查證方法的疏漏，記取教訓：盤點文字內容要注意同義詞，不能只靠幾個關鍵字。

## 這次做的修正（`server/data/strategies.ts`）

- **順流（institution-tailwind）**：觸發文字加上「停損：跌破支撐區失效位 X，或法人由買轉賣，兩者任一即出場」
- **潮間帶（range-fade）**：觸發文字加上「停損：跌破箱體下緣 X，箱體結構失效即出場」
- **破浪（breakout-pullback）**：觸發文字加上具體失效位數字（`f.invalidation`）
- **潮流（trend-follow）**：觸發文字加上具體 HL 數字（`lastLow`）

## 已知限制：這 4 個修正沒有專屬測試驗證「觸發時真的會顯示這段文字」

修正本身風險低——只是把已經算好、已經在觸發判斷裡用到的變數（`f.invalidation`／`lastLow`／`rangeLow`）字串化印出來，沒有動任何判斷邏輯，`tsc` 型別檢查通過。但沒有實際建構出對應的 K 線資料跑過 `evaluateStrategy()` 驗證這 4 個戰法各自的「triggered」路徑——建構這 4 個戰法個別要求的多頭反轉 K 線形態成本較高（每個戰法的進場條件組合不同）。這個測試缺口**在這次修正之前就存在**（`levels.test.ts` 只測過 `breakout-pullback` 的 `buildMarketFacts` 部分，沒測過完整 `evaluateStrategy` 觸發路徑；`institution-tailwind`／`trend-follow`／`range-fade` 三個完全沒有任何測試），不是這次修正才產生的。

使用者確認先記錄成技術債，不繼續投入時間補測試。

## 待辦：移動停利（trailing stop）

盤點時發現：全系統（研判引擎＋9個戰法）都只有「靜態的一次性停損/失效位」，沒有「隨獲利擴大動態上移停損」的移動停利機制。移動停利天然需要知道「進場價」——這剛好跟已經做好的「水池成本價」功能（`MEMORY-watchlist-cost-price.md`）是同一個問題的兩個面向，之後若要做移動停利，可以直接用水池的 `costPrice` 當基準點，不用另外設計一套進場價追蹤機制。這次沒有動手做，只記錄方向。

## 驗證

- `tsc --noEmit` 0 錯誤、`vitest run` 47/47 全過（不受影響，這4個修正沒有對應的新增/既有測試會執行到改動的那幾行 note 文字）
