---
name: strategy-unification
description: guliu-radar 研判頁真正接上九戰法系統，取代對不上名字的簡化版4戰法，並補上直白支撐壓力說明
metadata:
  type: project
---

## 背景（2026-09-12）：發現兩套平行、互不相干的戰法系統

討論研判結果呈現方式時查證發現：`StockDetail.tsx`（真正使用者看到的研判頁）顯示的「戰法匹配度」，資料來自 `server/api.ts` 裡一段**完全獨立、寫死的簡化邏輯**（`buildStrategyMatches()`），只有 4 個項目：「順勢回調買入」「突破確認」「均線站穩」「超跌反彈」。

這 4 個名字跟這整個對話認真開發、測試、剛補完停損邏輯的 9 個戰法（聲納/破浪/順流/潮流/潮間帶/回湧/破堤/鯨躍/洋流，`server/data/strategies.ts`）完全對不上——是兩套平行存在、互相不知道對方的系統。

**連帶發現一個之前做的功能其實從沒真的生效過**：掃描頁「選戰法（例如回湧）→ 點進研判頁自動捲動、強調對應那一列」（`MEMORY-scan-to-pa-strategy-highlight.md`），靠 `s.name === highlightStrategy` 比對——但研判頁的清單裡從來不會出現「回湧」這個名字（那份清單是那 4 個簡化版），這個強調捲動功能從做出來那一刻就沒有真的生效，我當時沒有抓到。

## 用具體例子確認差距

同一檔股票、同一組資料：
- 簡化版：「順勢回調買入：等待中 33% ——趨勢向上，等回調到支撐區再考慮」（模糊、沒有停損、比例算法粗略）
- 九戰法版：「破堤：✅已觸發——空間（3%帶量）或時間（3天站穩）擇一確認。失效：收盤跌破MA21 3%或連3天收不回，全面出場」（具體、有停損、有名稱故事）

## 決定：整合，但只保留有參考性的文字

使用者要求：整合兩套系統（改成真的顯示九戰法），但不要把 `evaluateStrategy()` 完整的 met/missing 技術細節全部倒出來——只保留「有參考性」的文字，像支撐壓力這種直白的價格行為資訊。

## 實作

| 檔案 | 變更 |
|---|---|
| `server/api.ts` | `buildStrategyMatches()` 整個重寫：迴圈跑過真正的 `STRATEGIES`（9個），呼叫 `evaluateStrategy()`，取代原本 4 個寫死的簡化條件。新增 `headline()` 輔助函式，把 `note` 在第一個句號前截斷，只保留「發生了什麼」的標題句，技術性的失效/停損細節不放進這個精簡清單（那些細節本來就在「掃描」頁的候選清單展開時看得到，那裡才是「比較多檔」的情境，這裡是「看懂這一檔」的情境，資訊密度本來就該不同） |
| `client/src/pages/StockDetail.tsx` | 「價格位置」卡片下方新增直白的支撐/壓力說明文字：「支撐 X–Y：這裡是買方目前願意接手的價位帶，跌破代表買方力道轉弱⋯」「壓力 X–Y：這裡是賣方目前還在出貨的價位帶，站穩突破代表多方力道增強」——用的是本來就有的 `facts.support`／`facts.resistance` 數字，只是加上白話解釋，不是新算出來的數字 |

## 副作用：之前失效的「戰法強調捲動」功能，這次順帶修好了

因為現在兩邊戰法名字系統統一了，`goPA(symbol, strategyName)` 傳過去的名字（例如「回湧」），現在真的會在 `StockDetail.tsx` 的 `signal.strategies` 清單裡找得到對應項目，捲動強調的邏輯不用再改，自動就生效了。

## 驗證

- `tsc --noEmit` 0 錯誤、`vitest run` 56/56 全過（不受影響，這次沒有新增測試檔案，`headline()` 用真實 note 樣本手動跑過驗證輸出合理，沒有寫成正式單元測試）
- 沒有瀏覽器實測（沙盒無瀏覽器能力），使用者本機測試時，最該檢查的兩件事：①研判頁「戰法匹配度」清單是不是變成九個真名字；②從掃描頁選一個戰法點進研判頁，是不是真的會捲動並強調對應那一列

## 後續更新：只保留有明確結論的戰法＋掃描強調例外（同一天）

使用者要求「簡潔明確」：清單只留有明確結論的（觸發＝符合／不適用），等待中跟資料不足都不顯示；數字改用 100%／0% 二元呈現（不用 met/missing 算出來的模糊比例——不適用的情況met/missing 陣列填法不一致，算出來的比例會誤導人）。

過濾之後浮現一個邊界情況：如果使用者從「掃描」頁點的候選股，剛好在那個戰法上是「等待中」狀態，跳進研判頁後這個戰法會被濾掉，「自動捲動強調」會找不到東西可捲——**這個功能又會看起來像壞掉**。

**修法**：加一個「掃描強調例外」——`/api/stocks/:symbol/signal` 多接受一個 `?highlight=戰法名稱` 參數，一路串到 `buildStrategyMatches()`；如果某個戰法剛好是使用者從掃描頁選的那個，就算狀態是「等待中」也破例保留在清單裡，不會被過濾規則吃掉。`fetchStockSignal()`／`StockDetail.tsx` 也跟著把 `highlightStrategy` 傳過去。

驗證：`tsc --noEmit` 0 錯誤、`vitest run` 56/56 全過。
