# 股流 Radar｜顧問審查筆記（給主導 AI 與產品負責人）

**日期：** 2026-09-06
**審查對象：** 未提交的在製品（commit 22de104 之後的工作樹）：`server/data/strategies.ts`（B3 策略層 v1）、`server/api.ts`（/strategies、/strategy/evaluate）、Home.tsx SVG 泡泡圖、watchlist 分組
**顧問結論：** ✅ 健康可繼續。tsc 全過、重用 shared/levels 事實層、四態誠實輸出（triggered/waiting/no-trade/insufficient）、文案符合合規邊界。以下 4 點建議提交前考慮。

---

## 1. breakout-pullback 的死條件（小）
`strategies.ts:108` 的 `f.resistance.touches >= 1` 恆為真（touches 最小值就是 1），建議移除，或改成實質條件（例如 `touches >= 2`，或之後有量能資料時加「突破日量能 ≥ 1.5× 均量」）。
另外該策略的語意是「close ≥ support.high」＝前壓力轉支撐後的站立確認，邏輯對，但變數名叫 `brokeOut` 容易讓日後維護者誤解，建議改名 `holdingAboveFormerHigh`。

## 2. getFacts 的限流風險（重要，Phase 2 前必讀）
`getFacts` 逐檔打 Yahoo（6mo 日線、10 分鐘快取）。單檔評估沒問題，但之後做「自選股 × 策略批次掃描」時，幾十檔輪詢會撞免費源限流。硬約束：掃描只限白名單（自選股／熱區前 N 檔），加上請求間隔與失敗退避。這也呼應 roadmap：真正的歷史累積要等 B0（SQLite＋每日管線）。

## 3. 順流（institution-tailwind）的門檻設計（建議）
目前「法人買超 ≥ 1 億」對高價股太鬆、對小型股太緊。B0 之後建議改為相對門檻：`買超金額 ÷ 該股當日成交值` 的比例，或「買超張數 ÷ 流通股數」，才跨股票可比。

## 4. range-fade 缺 direction 欄位（小）
潮間帶上緣目前回 waiting（做多不成立）。之後做空方條件時，`StrategyEval` 需要加 `direction: "long" | "short"` 欄位，否則推播無法標註方向。

---

## 運維提醒
- **API（3001）目前沒有行程**，前端落在「離線示範資料」模式（左側顯示 DEMO）。主導方把 `pnpm dev`（或 啟動股流Radar.bat）跑起來即可恢復真實資料。
- **回饋收件匣慣例建議**：處理完的回饋檔請移到 `feedback-inbox/processed/`，這樣左下角「標註回饋」的紅色角標（待處理數量）會歸零，使用者才知道「已消化」。目前收件匣還留著 09/06 11:51 的「XY軸異常」回饋檔（已由 SVG 泡泡圖解決，可歸檔）。
- 使用者的「標註回饋」工作流已可用：應用內畫圈→送出→JSON 進收件匣→讀檔處理→歸檔。

## 角色分工（2026-09-06 由產品負責人裁定）
- **主導 AI**：寫 code、提交、跑 dev 環境
- **顧問 AI（本檔作者）**：讀標註回饋、審查 commit 與資料正確性、架構與合規顧問；不直接改 repo 檔案
- 切換或交接時以 git commit 為準，避免工作樹互相覆蓋
